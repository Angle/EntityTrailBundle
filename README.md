# audit-bundle
Symfony bundle that automatically records who changed what data and when, for any Doctrine-managed entity. Drop-in replacement for per-entity changelog tables. Provides an opt-out attribute, configurable field exclusions, and an optional admin UI.
